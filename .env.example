ANTHROPIC_API_KEY=your-api-key-here

# Optional. Only needed to test the public-deployment embedding path locally
# (see README > Deploying). Without this, embeddings run locally for free
# via sentence-transformers - the normal setup for local/CLI use.
# VOYAGE_API_KEY=your-voyage-key-here
